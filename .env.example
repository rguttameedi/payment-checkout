# ============================================
# RENT PAYMENT APPLICATION - ENVIRONMENT VARIABLES
# ============================================
# Copy this file to .env and fill in your actual values
# NEVER commit .env to git (it's in .gitignore)
# ============================================

# Server Configuration
NODE_ENV=development
PORT=3000
API_BASE_URL=http://localhost:3000

# Database (PostgreSQL)
# Option 1: Full connection string
DATABASE_URL=postgres://username:password@localhost:5432/rent_payment_db

# Option 2: Individual variables (if not using DATABASE_URL)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=rent_payment_db
DB_USER=your_db_username
DB_PASSWORD=your_db_password

# JWT Authentication
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-min-32-chars
JWT_EXPIRES_IN=7d

# Cybersource Configuration (from existing setup)
CYBERSOURCE_MERCHANT_ID=9059034_1770903917
CYBERSOURCE_API_KEY_ID=19bb79cc-59aa-4a28-b5c9-2fa086d3c50e
CYBERSOURCE_SECRET_KEY=YMdMdBCLdgFmchmIEMWRgnW/9mr7Nge4legk3Efmtvs=
CYBERSOURCE_ENVIRONMENT=apitest.cybersource.com

# For production, use:
# CYBERSOURCE_ENVIRONMENT=api.cybersource.com

# Email Service (Nodemailer with Gmail)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=your-app@gmail.com
EMAIL_PASSWORD=your-app-password
EMAIL_FROM=Rent Payment App <noreply@rentpayapp.com>

# Frontend URL (for CORS)
CLIENT_URL=http://localhost:3001

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Session/Cookie Settings
SESSION_SECRET=your-session-secret-change-this-in-production

# Logging
LOG_LEVEL=info

# ============================================
# PRODUCTION DEPLOYMENT (Render.com)
# ============================================

# When deploying to Render:
# 1. Set NODE_ENV=production
# 2. DATABASE_URL will be auto-provided by Render PostgreSQL
# 3. Add JWT_SECRET via Render dashboard (use "Generate Value")
# 4. Add all CYBERSOURCE_* variables as secrets
# 5. Set CLIENT_URL to your frontend URL
# 6. Configure EMAIL_* variables with production email service

# ============================================
# DEVELOPMENT NOTES
# ============================================

# Local PostgreSQL setup:
# 1. Install PostgreSQL: https://www.postgresql.org/download/
# 2. Create database: createdb rent_payment_db
# 3. Run migrations: psql -d rent_payment_db -f database/migrations/001-create-users.sql
# 4. Continue with 002-007 migration files
# 5. Seed data: psql -d rent_payment_db -f database/seeds/sample-data.sql

# Generate JWT_SECRET:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"

# Gmail App Password (for EMAIL_PASSWORD):
# 1. Enable 2FA on your Gmail account
# 2. Go to https://myaccount.google.com/apppasswords
# 3. Generate app password for "Mail"
# 4. Use that password here (not your regular Gmail password)
